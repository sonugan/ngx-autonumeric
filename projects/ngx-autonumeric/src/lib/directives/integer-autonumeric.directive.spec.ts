import { IntegerAutonumericDirective } from './integer-autonumeric.directive';

describe('IntegerAutonumericDirective', () => {
  it('should create an instance', () => {
    const directive = new IntegerAutonumericDirective();
    expect(directive).toBeTruthy();
  });
});
